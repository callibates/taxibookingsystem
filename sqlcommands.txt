CREATE TABLE requests(booknum int, custname varchar(20), phone varchar(20), address varchar(20), suburb varchar(20), destination varchar(20), pickupdate datetime, currdate date, currtime time, status varchar(20), primary key(booknum));